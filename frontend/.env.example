# API Express + PostgreSQL (Kwetu Garage)
# Copiez vers .env.local :  cp .env.example .env.local

# URL du backend Express (port 3001 par défaut)
BACKEND_URL=http://localhost:3001

# Appels axios côté navigateur : /api est proxifié vers BACKEND_URL par next.config.js
NEXT_PUBLIC_API_URL=/api
